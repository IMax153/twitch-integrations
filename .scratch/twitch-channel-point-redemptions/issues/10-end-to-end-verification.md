# 10: End-to-end verification in production

**What to build:** Prove the whole feature on the deployed `production` stage, in the shape of OAuth ticket 09. Deploy, confirm the receiver's route wins over the API Worker's custom domain and that Access does not challenge it, reconnect Twitch from the Broadcaster Page and watch reconcile create the Song Request Reward and three enabled Event Subscriptions with the real challenge handshake, then exercise the feature live: go Live and see the Reward unpause, redeem Song Request from a second Twitch account with a track link and hear it queue with the chat reply, redeem with a bad link and see the refund and reply, go Offline and see the Reward pause. Record every observation and any surprise on this ticket.

**Blocked by:** 08, 09

**Status:** ready-for-human

- [ ] `alchemy deploy --stage production` completes and the plan shows the receiver Worker, its route, and no change to the Access application
- [ ] An unauthenticated GET to the receiver's route returns the empty 404 rather than an Access login page; a GET to the API Worker still gets Access
- [ ] After reconnecting Twitch, Get Custom Reward shows the Song Request Reward with the spec's settings and Get EventSub Subscriptions shows the three subscriptions enabled
- [ ] Going Live unpauses the Reward in the Twitch dashboard; going Offline pauses it
- [ ] A Song Request with a valid track link while Live queues the track on Spotify, is marked fulfilled, and gets the success chat reply
- [ ] A Song Request with a non-track link is refunded and gets the refund chat reply
- [ ] Observations, timings, and any deviation from the spec are recorded in a comment on this ticket

## Comments

Note from ticket 09 (2026-09-12): the deploy now creates a zone rate-limiting rule (`EventSubRateLimit`) and adopts the `minbadblue.com` zone. Two prerequisites before the first box: the Alchemy Cloudflare profile carries `zone.read` but no `zone-waf` scope, so run `alchemy profile edit` and add `zone-waf.write` first, or the ruleset's `PUT` fails with an authentication error the plan cannot surface. The rule's expression uses `starts_with` on the path; the Free plan's dashboard offers "starts with" on the path field, but only the deploy confirms the API accepts it, and the fallback is `http.request.uri.path eq "/eventsub/twitch"`. The resource owns the zone's whole `http_ratelimit` phase, so any rule made by hand in that phase is replaced; check the dashboard's rate limiting rules once before deploying if one might exist. Expect `[Zone] adopted` and `[EventSubRateLimit] create` in the plan alongside the Worker updates.

Agent work done on 2026-09-12 from the worktree; the deploy and every box remain the Broadcaster's, so the ticket stays `ready-for-human`. What was checked read-only, in the order the boxes run:

- `alchemy plan --stage production` from the worktree at `c92b53b`: 1 to create, 3 to update, 1 adopted. `[EventSubRateLimit] create`, `[Zone] adopted`, the three Workers as updates, and every Access resource (`OperatorWorkersAccess`, its two enrollments, `AllowTwitchChannelOwner`, `GitHubIdentityProvider`) a noop, as the first box expects.
- The Alchemy Cloudflare profile still carries no `zone-waf` scope (its list ends at `zone.read`, `access.write`, `ssl-and-certificates.write`, `memberships.read`), so the prerequisite from ticket 09 is unmet: run `pnpm exec alchemy profile edit` and add `zone-waf.write` before the deploy, or it fails at the ruleset's `PUT`. The README's deployment section now says so.
- The live receiver route, anonymously with Node `fetch`: `GET /eventsub/twitch` and `GET /eventsub/nope` answer an empty 404 with no Access redirect; an unsigned `POST /eventsub/twitch` answers an empty 403; `GET /`, `/setup`, and `/setup/api/connections` answer 302 to the `cloudflareaccess.com` login. The second box already holds on the deployed shape; the deploy changes the receiver's code, not its route or enrollment, so it should keep holding.
- Twitch CLI `api get eventsub/subscriptions` under an app token: three subscriptions, `channel.channel_points_custom_reward_redemption.add`, `stream.online`, `stream.offline`, all `enabled`, all with callback `https://stream.minbadblue.com/eventsub/twitch`, created at 17:42Z on 2026-09-12. That is the reconnect from the ticket 04 and 05 verification, four minutes before #7 merged, so production runs code from before #8 through #11: Song Requests, refunds, held cancellations, and the rate limit have never been deployed. The third box needs the fresh reconnect anyway, since reconcile deletes and recreates the subscriptions, and the Reward check needs a user token the agent has no way to get; the README gives the CLI command.
- Not checked: whether the Free plan accepts `starts_with` in the rule's expression, and whether the `http_ratelimit` phase already holds a hand-made rule; both need the missing scope. If the deploy rejects the expression, the fallback is `http.request.uri.path eq "/eventsub/twitch"` in `apps/eventsub/src/RateLimit.ts`.

Defect found and fixed here: `alchemy state ls` and `state read` open the stack under Alchemy's built-in `placeholder` stage, the one its non-deploying commands use, and the stage guard from OAuth ticket 10 refused it, so the state commands had been unusable since that guard landed. `guardStage` now admits that stage, with `apps/infra/test/Stage.test.ts` covering production, the `dev_` stages under `alchemy dev`, the placeholder, and refusal of anything else. `state ls TwitchIntegrations/production -r` then lists the six resources plus the output; `alchemy logs` takes `--stage` and was never affected. OAuth ticket 10's first box, which promised refusal of every other name, carries a comment amending it: a typed `--stage placeholder` on a deploy would now pass, since the guard has no way to tell a deploy from a state read.

Human steps, in order: add the scope; `vp run deploy` and confirm the plan matches the shape above; rerun the anonymous probe from the README's step 4; reconnect Twitch on the Broadcaster Page and read the logs and the subscription list; go Live and Offline and watch the Reward's pause state in the dashboard; redeem from a second account with a track link, then with a non-track link. Record timings and surprises below.

First deploy attempt (2026-09-12, 17:54 local): failed, and not for the expected reason. `alchemy profile edit` re-authorized the profile with `zone-waf.write` but also re-selected the Cloudflare account: the profile's `accountId` went from the personal account (`17f1cec1…`, which holds `minbadblue.com` and production) to "Ma@matechs.com's Account" (`a5324318…`), the only account the new token can see. Alchemy's state store is per account, so the deploy saw no state and planned every resource as `create`; the zone lookup found no `minbadblue.com` there and the create was refused for lack of `zone.create`; the three Workers failed at their routes with "zone not found". Production is untouched: the anonymous probe answers exactly as before the deploy, and the personal account's state still lists its six resources once the profile points back at it. Left behind in the matechs account: three Workers (`twitchintegrations-eventsub-prkazbh…`, `twitchintegrations-web-production-uranjze…`, `twitchintegrations-worker-production-nkd4xz…`), an Access application "Twitch Integrations" with no domain, a policy "Allow Twitch channel owner" admitting the Broadcaster's email, a GitHub identity provider carrying the Access GitHub OAuth application's credentials, and a `TwitchIntegrations/production` stack in that account's state store with `Zone` and `EventSubRateLimit` stuck in `creating`. Recovery: destroy that stack while the profile still points at the matechs account, re-run `alchemy profile edit` choosing the personal account, confirm `alchemy plan` shows updates and noops again, then deploy.

Second deploy (2026-09-12, 18:06 local, personal account): the plan matched the expected shape, the three Workers updated, the receiver's route reconciled, and the zone adopted. The first box holds except for the rule: `EventSubRateLimit` failed with Cloudflare's `'[ip.src]' is not a valid value for characteristics because characteristics field is missing 'cf.colo.id'`, since rate limiting is counted per colocation. Neither the plan nor the Free-plan availability table mention it; the dashboard adds the colocation key silently. Fixed by keying the rule on `cf.colo.id` and `ip.src` in `apps/eventsub/src/RateLimit.ts`, with the spec and ADR saying the counter is per colocation. The `starts_with` expression was not what failed, though the API may validate the characteristics before the expression, so the fallback stays on record. The state entry is stuck in `creating` and the next deploy retries it; the worktree plan after the fix is one create, everything else noop, so the retry touches nothing but the rule. Production now runs the Song Request code, so the reconnect and the live steps can proceed in parallel with the rule.
